name = "otoscopy-admin"
main = "worker.js"
compatibility_date = "2026-08-01"

# Non-secret vars — set these directly here or via `wrangler` dashboard
[vars]
GITHUB_OWNER = "jakalnz"
GITHUB_REPO = "otoscopy"
GITHUB_BRANCH = "main"

# Secrets — do NOT put these here. Set them with:
#   wrangler secret put ADMIN_PASSWORD
#   wrangler secret put GITHUB_TOKEN
